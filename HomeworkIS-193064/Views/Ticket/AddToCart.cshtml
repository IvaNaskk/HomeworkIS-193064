@model HomeworkIS_193064.Domain.DTO.AddToShoppingCartDto

@*<h1>The ticket for "@Model.Movie.Title" at @Model.Date is added to Reservations. Please confirm your reservation and pay for it.</h1>
<td><a type="button" class="btn btn-outline-primary">Reservations</a></td>*@
<h1>Add selected product to shopping cart</h1>
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddToShoppingCart">
            <input type="hidden" asp-for="TicketId" />
            <div class="form-group">
                <input disabled class="form-control" asp-for="SelectedTicket.Id" />
            </div>
            <div class="form-group">
                <input type="number" class="form-control" asp-for="Quantity" />
            </div>
            <div class="form-group">
                <input type="submit" value="Reservation" class="btn btn-primary"/>
            </div>
    </form>
</div>
</div>
